const age = 20

if(age <= 18) {
    console.log("you are under age")
} else {
    console.log("you are older")
}

let isMember=false
// let discount = ""
// if (isMember) {
//     discount= 0.20
// } else {
//     discount= 0.05
// }
// console.log(discount)

let marriedMessage = ""

if(isMarried) {
    marriedMessage = "is married.~"
} else {
    marriedMessage = "is not married."
}

let marriedMessage1 = isMarried ? "is married. " : "is not married."



let discount = isMember ? 0.20 : 0.05;
console.log(discount)